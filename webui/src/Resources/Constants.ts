export const PRIMARY_COLOR: string = '#4a7300'

export const VARIABLE_UNKNOWN_VALUE = '$NA'

/**
 * Build identity. Change these two values to rebrand the build - every visible product name and
 * window title in the web UI derives from them.
 *
 * Note that this renames *this build*, not the upstream project: the sponsorship and community links
 * still say "Bitfocus Companion", because they genuinely point at Bitfocus.
 */
export const PRODUCT_VENDOR = 'STACKER'
export const PRODUCT_NAME = 'Companion'
export const PRODUCT_FULL_NAME = `${PRODUCT_VENDOR} ${PRODUCT_NAME}`
